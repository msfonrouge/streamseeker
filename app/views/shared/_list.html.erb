  <div class="" data-search-target="list">
    <ul class="list-group bring-closer-search1">
      <% movies.each do |movie| %>
        <li id="search_dropdown_results" class="list-group-item d-flex">
          <%= cl_image_tag movie.photo.key, height:50, width: 40, crop: :thumb%>
          <%= link_to movie.title, movie_path(movie), class: "list-group-item list-group-item-action", id: "search-dropdown-description" %>
        </li>
      <% end %>
    </ul>
  </div>
